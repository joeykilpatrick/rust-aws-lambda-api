{
  "name": "rust-aws-lambda-api",
  "version": "0.0.1",
  "description": "An example API with Rust's Rocket framework running on AWS Lambda.",
  "scripts": {
    "clean": "rm -rf cdk.out && rm -rf node_modules && rm -rf rust/target",
    "build": "cd rust && ./build.sh",
    "deploy": "cdk deploy"
  },
  "author": "Joey Kilpatrick",
  "license": "MIT",
  "dependencies": {
    "aws-cdk": "^2.59.0",
    "aws-cdk-lib": "^2.59.0",
    "class-transformer": "^0.5.1",
    "class-transformer-validator": "^0.9.1",
    "class-validator": "^0.14.0",
    "constructs": "^10.1.211",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "ts-node": "^10.9.1"
  }
}
